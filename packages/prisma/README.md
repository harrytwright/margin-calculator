## Prisma

The home for the prisma schema. Whilst everything will be run through Kysely, both the client and migrations, this is 
where the schema lives and where the types are generated from.

> Prisma does not handle any migrations

### Building Locally

```
pnpm build
```
